spring.application.name=clinica-shiba

# Server
server.port=${PORT:8090}

# PostgreSQL Database (for development - using same as production)
spring.datasource.url=jdbc:postgresql://dpg-d0pphc6uk2gs739tpc60-a.oregon-postgres.render.com:5432/clinica_shiba_db
spring.datasource.username=clinica_shiba_db_user
spring.datasource.password=vt41tuwta2hhIVTUgekbLrfx8liLxiWa
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Logging
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Actuator
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

# Swagger/OpenAPI
springdoc.api-docs.version=openapi-3-0
springdoc.swagger-ui.path=/swagger-ui.html

# CORS
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:4200,http://localhost:3000}

# Archivos subidos
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
